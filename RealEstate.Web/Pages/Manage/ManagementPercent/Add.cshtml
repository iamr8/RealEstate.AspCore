@page
@model AddModel
@{
    var employees = await EmployeeService.ListAsync(false).ConfigureAwait(false);
    var emploo = employees?.Select(x => new SelectListItem
    {
        Value = x.Id,
        Text = $"{x.FirstName} {x.LastName}",
        Selected = Model.NewManagementPercent?.EmployeeId == x.Id
    });
}
<form method="post" class="col-12">
    <div class="widget">
        <widget-header title="@Model.PageTitle"></widget-header>
        <input asp-for="NewManagementPercent.Id" />
        <section class="container-fluid">
            <div class="row justify-content-center align-items-center">
                <div class="col-xs-12 col-6 margin-bottomed">
                    <label asp-for="NewManagementPercent.Percent" class="control-label"></label>
                    <input asp-for="NewManagementPercent.Percent" class="form-control" />
                    <span asp-validation-for="NewManagementPercent.Percent" class="text-danger"></span>
                </div>
                <div class="col-xs-12 col-6 margin-bottomed">
                    <label asp-for="NewManagementPercent.EmployeeId" class="control-label"></label>
                    <select asp-items="emploo" asp-for="NewManagementPercent.EmployeeId" class="form-control"></select>
                    <span asp-validation-for="NewManagementPercent.EmployeeId" class="text-danger"></span>
                </div>
            </div>
        </section>
        <widget-footer>
            <widget-footer-text>
                <div asp-validation-summary="All">
                    @Model.ManagementPercentStatus
                </div>
            </widget-footer-text>
            <widget-footer-button>
                <input type="submit" value="@SharedLocalizer["Submit"]" class="btn-success" />
            </widget-footer-button>
        </widget-footer>
    </div>
</form>