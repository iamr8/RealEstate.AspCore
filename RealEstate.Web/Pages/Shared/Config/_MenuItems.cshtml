<nav-button page="@(User.Identity.IsAuthenticated ? $"/{nameof(Manage)}/Index" : "/Index")" icon="~/icons/statistics.svg" subject="@(User.Identity.IsAuthenticated ? SharedLocalizer["Statistics"] : SharedLocalizer["HomePage"])"></nav-button>
@* <nav-button page="/@nameof(Manage)/@nameof(Manage.Notification)/Index" icon="~/icons/notification.svg" subject="@SharedLocalizer["Notifications"]"></nav-button> *@

@if (User.Identity.IsAuthenticated)
{
    @*<nav-button page="/@nameof(Manage)/@nameof(Manage.Item)/Index" icon="~/icons/deal.svg" subject="@SharedLocalizer["Deals"]">
        @if (User.IsInRole(Role.Admin.ToString()) || User.IsInRole(Role.SuperAdmin.ToString()))
        {
            <nav-button page="/@nameof(Manage)/@nameof(Manage.Deal)/Index" icon="~/icons/dealstatus.svg" subject="@SharedLocalizer["UpdateDealStatus"]"></nav-button>
        }
    </nav-button>
    <nav-button page="/@nameof(Manage)/@nameof(Manage.Property)/Index" icon="~/icons/property.svg" subject="@SharedLocalizer["Properties"]">
        @if (User.IsInRole(Role.Admin.ToString()) || User.IsInRole(Role.SuperAdmin.ToString()))
        {
            <nav-button page="/@nameof(Manage)/@nameof(Manage.Facility)/Index" icon="~/icons/possibility.svg" subject="@SharedLocalizer["Facilities"]"></nav-button>
            <nav-button page="/@nameof(Manage)/@nameof(Manage.District)/Index" icon="~/icons/map.svg" subject="@SharedLocalizer["Districts"]"></nav-button>
        }
    </nav-button>

    if (User.IsInRole(Role.Admin.ToString()) || User.IsInRole(Role.SuperAdmin.ToString()))
    {
        <nav-button page="/@nameof(Manage)/@nameof(Manage.Feature)/Index" icon="~/icons/features.svg" subject="@SharedLocalizer["Features"]"></nav-button>
    }
    <nav-button page="/@nameof(Manage)/@nameof(Manage.Person)/Index" icon="~/icons/persons.svg" subject="@SharedLocalizer["Persons"]"></nav-button>
    <nav-button page="/Manage/Financial/Index" icon="~/icons/financial.svg" subject="@SharedLocalizer["Financial"]"></nav-button>*@

    if (User.IsInRole(Role.Admin.ToString()) || User.IsInRole(Role.SuperAdmin.ToString()))
    {
        <nav-button page="/@nameof(Manage)/@nameof(Manage.User)/Index" icon="~/icons/user.svg" subject="@SharedLocalizer["Users"]"></nav-button>
    }

}